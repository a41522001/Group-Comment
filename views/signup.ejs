<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>社群留言系統</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        .wrap{
            padding-top: 10px;
            width: 100%;
            max-width: 1200px;
            margin: auto;
            height: 100vh;
            display: flex;
            align-items: center;
            flex-direction: column;
            gap: 20px;
            background-image: linear-gradient(90deg, #FF6F61, #6B5B95);
        }
        .wrap h1{
            margin: 30px;
            color: #fff;
        }
        .wrap form{
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .wrap form label{
            margin: 20px 0 10px 0;
            font-size: 1.35rem;
            color: #fff;
        }
        .wrap form .name-wrap,
        .wrap form .email-wrap,
        .wrap form .password-wrap{
            display: flex;
            flex-direction: column;
        }
        .wrap form input{
            height: 30px;
            padding: 15px 20px;
            border-radius: 10px;
            border: none;
            outline: none;
        }
        .wrap form button{
            margin-top: 30px;
            text-decoration: none;
            color: #fff;
            background-color: #145c8b;
            border: none;
            border-radius: 8px;
            font-weight: 700;
            cursor: pointer;
            font-size: .9rem;
            padding: 8px 70px;
            transition: all .2s ease-in;
        }
        .wrap form  button:hover{
            transform: scale(1.1);
            opacity: .7;
        }
    </style>
</head>
<body>
    <div class="wrap">
        <h1>註冊會員</h1>
        <form action="/signup-success" method="POST">
            <div class="name-wrap">
                <label for="name">姓名</label>
                <input type="text" name="name" id="name">
            </div>
            <div class="email-wrap">
                <label for="email">信箱帳號</label>
                <input type="text" name="email" id="email">
            </div>
            <div class="password-wrap">
                <label for="password">密碼</label>
                <input type="password" name="password" id="password">
            </div>
            <button>註冊會員</button>
        </form>
    </div> 
</body>
</html>